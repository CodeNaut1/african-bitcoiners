// Placeholder — will be replaced with the AB Newsletter Archive block in Step 13
import type { Block } from 'payload'

export const Archive: Block = {
  slug: 'archive',
  interfaceName: 'ArchiveBlock',
  fields: [],
  labels: { plural: 'Archives', singular: 'Archive' },
}
