// Placeholder — form builder not used; formBuilderPlugin removed
import type { Block } from 'payload'

export const FormBlock: Block = {
  slug: 'formBlock',
  interfaceName: 'FormBlock',
  fields: [],
  labels: { plural: 'Form Blocks', singular: 'Form Block' },
}
